/*

public class TestFooMaker {

    FooMaker maker = new FooMaker();

    @Test
    public void testDayOfWeekOk() {
        maker.getDayOfWeek();
        assertEquals("The day of week is correct!", Calendar.DAY_OF_WEEK, maker.getDayOfWeek());
    }

    @Test
    public void testDayOfWeekFail() {
        maker.getDayOfWeek();
        assertEquals("The day of week is INcorrect!", Calendar.DAY_OF_WEEK-1, maker.getDayOfWeek() )
    }

    @Test
    public void testDayOfWeekNextOk() {
        maker.getDayOfWeek(2);
        assertEquals("The day of week is correct!", (Calendar.DAY_OF_WEEK + 2), maker.getDayOfWeek(2));
    }

    @Test
    public void testDayOfWeekNextFail() {
        maker.getDayOfWeek(2);
        assertEquals("The day of week is INcorrect!", (Calendar.DAY_OF_WEEK + 3), maker.getDayOfWeek(2));
    }

    @Test
    public void testTheNextDayOfWeekOk(){
        maker.getNextDayOfWeek();
        assertEquals("The day of week is correct!", (Calendar.DAY_OF_WEEK + 1), maker.getNextDayOfWeek());
    }

    @Test
    public void testTheNextDayOfWeekFail(){
        maker.getNextDayOfWeek();
        assertEquals("The day of week is INcorrect!", (Calendar.DAY_OF_WEEK + 2), maker.getNextDayOfWeek());
    }

    @Test
    public void testDayFooOk(){
        maker.getDayFoo();
        assertEquals("The day of week is correct!", (Calendar.DAY_OF_WEEK + maker.getRandom), maker.getDayFoo());
    }
    @Test
    public void testDayFooFail(){
        maker.getDayFoo();
        assertEquals("The day of week is INcorrect!", (Calendar.DAY_OF_WEEK + 2), maker.getDayFoo());
    }

}*/
